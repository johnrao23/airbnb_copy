import React from "react";

const AIButton = () => {

    const handleClick = () => {
        
    }

    return (
        <div>
            <button>Ask Ai</button>
        </div>
    )
}

export default AIButton;